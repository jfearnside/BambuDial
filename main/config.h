#pragma once

/*
 * BambuDial — Compile-time constants only.
 * All user configuration (WiFi, printers, timezone) is stored in NVS
 * and configured via the web setup portal.
 */

/* Maximum number of printers supported */
#define MAX_PRINTERS    3
